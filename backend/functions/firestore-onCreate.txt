chat: [
    Append auth's id to chat's from
    Initialize fields (sentAt, readAt) in the created chat
    Append to to from's chattedWith array and vice versa
]

course: [
    Initialize fields (createdAt, updatedAt, savedBy) in the created course
    Append authorized user's id to userId field
]

discussion: [
    Append authorized user's id to userId field
    Initialize fields (createdAt) in the created discussion
]

question: [

]

session: [
    Initialize fields (createdAt) in the created session
]

user: [
    Add isStudent to roles
    Initialize fields (registeredAt, deletedAt, deactivatedAt, active, premium, image, chattedWith )
]
